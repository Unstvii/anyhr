import React from "react";
import { Typography, Box, Container } from "@mui/material";

const Hero = () => (
  <Container sx={{ mt: "10vh", mb: "2vh" }}>
    <Typography variant="h1" fontSize={"42px"}>
      Product Catalog
    </Typography>
  </Container>
);

export default Hero;
